import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        padding: 20,
        borderWidth: .5,
        borderColor: '#E6E6E6',
        backgroundColor: '#FFF'
    },
    label: {
        color: "#C1BCCC",
        fontSize: 10
    },
})

export default styles